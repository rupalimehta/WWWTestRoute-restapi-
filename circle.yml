---
## Customize test commands
machine:
  node:
    version:
      0.12.2
deployment:
  hub:
    branch: master
    commands:
      - docker login -e rupalime@cybage.com -u rupalimehta -p Kamlesh@1970
      - docker push rupalimehta/restapicd
test:
  pre:
    - npm start:
        background: true
    - sleep 5
  override:
    - mocha
     
